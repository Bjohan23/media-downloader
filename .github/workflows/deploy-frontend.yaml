name: Build and Deploy Media Downloader Frontend

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yaml'
  workflow_dispatch:
    inputs:
      rollback_manual:
        description: 'Forzar rollback a versión anterior'
        required: false
        default: false
        type: boolean

env:
  IMAGE_NAME: mediadownloader-frontend
  CONTAINER_NAME: mediadownloader-frontend
  NETWORK_PROXY: proxy_net
  PORT: 3000

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Construir imagen Docker
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
            -t ${{ env.IMAGE_NAME }}:latest ./frontend
          docker save ${{ env.IMAGE_NAME }}:latest | gzip > image.tar.gz

      - name: Copiar imagen al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "image.tar.gz"
          target: "/tmp"

      - name: Desplegar en servidor
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            set -e

            # --- CONFIGURACIÓN ---
            IMAGE_NAME="${{ env.IMAGE_NAME }}"
            CONTAINER_NAME="${{ env.CONTAINER_NAME }}"
            NETWORK_PROXY="${{ env.NETWORK_PROXY }}"
            PORT="${{ env.PORT }}"
            MAX_RETRIES=15
            RETRY_INTERVAL=3

            # --- FUNCIÓN: Diagnóstico ---
            diagnostico() {
              echo "=== DIAGNÓSTICO DE ERROR ==="
              docker ps -a --filter "name=$CONTAINER_NAME"
              docker logs "$CONTAINER_NAME" --tail 30 2>&1 || true
            }

            # --- FUNCIÓN: Conectar a red ---
            conectar_red() {
              docker network connect "$NETWORK_PROXY" "$CONTAINER_NAME" 2>/dev/null || true
            }

            # --- FUNCIÓN: Health Check ---
            health_check() {
              echo "Verificando salud en puerto $PORT..."
              for i in $(seq 1 $MAX_RETRIES); do
                echo "  Intento $i de $MAX_RETRIES..."
                if curl -sf --max-time 5 "http://127.0.0.1:${PORT}" > /dev/null 2>&1; then
                  echo "  ✓ Health check exitoso"
                  return 0
                fi
                sleep $RETRY_INTERVAL
              done
              echo "  ✗ Health check falló después de $MAX_RETRIES intentos"
              return 1
            }

            # --- FUNCIÓN: Ejecutar Contenedor ---
            run_container() {
              local TAG=$1
              docker run -d \
                --name "$CONTAINER_NAME" \
                --restart unless-stopped \
                -p ${PORT}:${PORT} \
                -e NODE_ENV="production" \
                -e NEXT_PUBLIC_API_URL="${{ secrets.NEXT_PUBLIC_API_URL }}" \
                -e TZ="America/Lima" \
                "$IMAGE_NAME:$TAG"
            }

            # --- PROCESO PRINCIPAL ---

            # 1. Manejo de Rollback Manual
            if [ "${{ github.event.inputs.rollback_manual }}" = "true" ]; then
              echo "🔄 Iniciando Rollback Manual..."
              docker stop "$CONTAINER_NAME" 2>/dev/null || true
              docker rm "$CONTAINER_NAME" 2>/dev/null || true
              run_container "backup"
              conectar_red
              echo "✓ Rollback completado"
              exit 0
            fi

            echo "[1/7] Cargando nueva imagen..."
            docker load < /tmp/image.tar.gz
            rm -f /tmp/image.tar.gz

            echo "[2/7] Gestionando Backup..."
            if docker images | grep -q "${IMAGE_NAME}:latest"; then
              docker tag "${IMAGE_NAME}:latest" "${IMAGE_NAME}:backup" 2>/dev/null || true
            fi

            echo "[3/7] Deteniendo contenedor anterior..."
            docker stop "$CONTAINER_NAME" 2>/dev/null || true
            docker rm "$CONTAINER_NAME" 2>/dev/null || true

            echo "[4/7] Iniciando nuevo contenedor..."
            run_container "latest"

            echo "[5/7] Conectando red..."
            conectar_red

            echo "[6/7] Verificando despliegue..."
            if health_check; then
              echo "✅ DESPLIEGUE EXITOSO"
              docker image prune -f 2>/dev/null || true
            else
              echo "❌ FALLÓ EL DESPLIEGUE, EJECUTANDO ROLLBACK..."
              diagnostico
              docker stop "$CONTAINER_NAME" 2>/dev/null || true
              docker rm "$CONTAINER_NAME" 2>/dev/null || true
              run_container "backup"
              conectar_red
              echo "🔄 ROLLBACK COMPLETADO"
              exit 1
            fi

            echo "[7/7] Limpieza completada"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "Frontend desplegado en puerto $PORT"
            echo "URL: https://mediadownloader.johandev.lat"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
